import { styler, ui } from './deps'

// set chart font
ui.chartConfig.font = '10px JetBrains Mono'
ui.chartConfig.fontSize = 10

// initialize the styler based on the css in the document's head
styler.init()
